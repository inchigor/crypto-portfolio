{
  "name": "crypto-portfolio",
  "version": "1.0.0",
  "description": "Local-first crypto portfolio tracker with manual holdings and read-only EVM wallet sync.",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "4.22.2"
  }
}
